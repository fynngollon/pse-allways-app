package com.pseteamtwo.allways.ui.routes

import android.view.View
import androidx.recyclerview.widget.RecyclerView

/**
 * Represents the ViewHolder for each item in the Routes list.
 * Defines how each route item is displayed in the RecyclerView.
 */
class RouteItemViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
}